meta {
  name: documents
  type: http
  seq: 6
}

get {
  url: {{base_url}}/products?join=documents&fields=documents
  body: none
  auth: none
}

params:query {
  join: documents
  fields: documents
}

settings {
  encodeUrl: true
  timeout: 0
}
