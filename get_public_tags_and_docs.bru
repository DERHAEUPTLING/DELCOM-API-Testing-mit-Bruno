meta {
  name: get_public_tags_and_docs
  type: http
  seq: 6
}

get {
  url: {{base_url}}/public/tags?join=documents
  body: none
  auth: inherit
}

params:query {
  join: documents
}

settings {
  encodeUrl: true
  timeout: 0
}
